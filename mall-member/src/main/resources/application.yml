spring:
  application:
    name: mall-member
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: 20f85521-d79d-4c9b-a038-29de85173330
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.116.101:3306/gulimall_ums?serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss


mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto

server:
  port: 8001